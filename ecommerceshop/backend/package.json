{
  "name": "ecommerceshop-backend",
  "version": "1.0.0",
  "description": "Node.js backend for Nature E-Commerce shop",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd .. && npm run build && cd backend && node -e \"const fs = require('fs'); const path = require('path'); if (fs.existsSync('../dist')) { fs.rmSync('./dist', {recursive: true, force: true}); fs.cpSync('../dist', './dist', {recursive: true}); console.log('✅ Frontend build copied to backend/dist'); }\"",
    "start:prod": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": ["ecommerce", "nature", "express", "nodejs"],
  "author": "Rinat Mukhamadullin",
  "license": "MIT"
}
